const num1 = parseInt(prompt('Enter a number which is greater or equal 49 either you will get an alert '));
if(num1 >= 49){
    let num2 = num1;
    while(num2 >= 20){
        let text;
        text += `<br> The number is + ${num2}`;
        //console.log(text);
    }
}
